{
  "name": "@garyo-test/common",
  "license": "MIT",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -d --outDir dist",
    "prebuild": "rm -rf dist",
    "postbuild": "yalc push"
  },
  "files": ["dist"]
}
